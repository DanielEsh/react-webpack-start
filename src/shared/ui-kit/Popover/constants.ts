import { Offset, Delay, Trigger } from './types'

export const DEFAULT_OFFSET: Offset = {
  side: 10,
  align: 10,
}

export const DEFAULT_DELAY: Delay = {
  enter: 0,
  leave: 200,
}

export const DEFAULT_TRIGGER_TYPE: Trigger = 'click'
